(ns weatherme.weather-test
  (:require [clojure.test :refer :all]
            [weatherme.core :refer :all]
            [weatherme.climbs :as climbs]))

(deftest get-routes-test
  (testing "The example is solved"
    (is (= 1 1))))

(deftest get-location-routes-test)
